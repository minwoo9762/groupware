<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himedia.groupware.dao.WorkBoardDao">
    <select id="getAllCount" resultType="_int">
        select count(*)from board;
    </select>

    <select id="selectBoard" parameterType="com.himedia.groupware.dto.Paging" resultType="com.himedia.groupware.dto.WorkBoardDto">
        select*from board order by id desc limit #{displayRow} offset #{startNum}
    </select>

    <select id="getBoard" resultType="com.himedia.groupware.dto.WorkBoardDto">
        select*from board where id=#{id}
    </select>

    <update id="addReadCount">
        update board set readcount = readcount + 1 where id =#{id}
    </update>

    <insert id="insert" parameterType="com.himedia.groupware.dto.WorkBoardDto">
        insert into board (userid, content, name, title, image, savefilename) values (#{userid}, #{content}, #{name}, #{title}, #{image}, #{savefilename})
    </insert>



</mapper>