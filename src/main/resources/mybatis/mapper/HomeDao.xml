<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.himedia.groupware.dao.HomeIDao">
    <select id="getVCT" parameterType="com.himedia.groupware.dto.VacationDto">
        select * from vacation where userid=#{id}
    </select>

</mapper>