<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himedia.groupware.dao.IInfoDao">
    <select id="getInfo" resultType="com.himedia.groupware.dto.InfoDto">
        select * from allinfo where name=#{name}
    </select>
</mapper>