<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.himedia.groupware.dao.AdminIDao">

    <select id="getAllCount" resultType="_int">
        select count(*) from user where name like concat('%', #{key}, '%');
    </select>
    <select id="getUser" resultType="com.himedia.groupware.dto.UserDto">
        select * from user where name like concat('%', #{key}, '%') or part like concat('%', #{key}, '%') order by id desc limit #{paging.displayRow} offset #{paging.startNum};
    </select>

    <update id="updateUser" parameterType="com.himedia.groupware.dto.UserDto">
        UPDATE user set part=#{part}, provider=#{provider}, state=#{state} WHERE id = #{id}
    </update>

    <select id="getAllCountNotice" resultType="_int">
        select count(*) from notice where title like concat('%', #{key}, '%');
    </select>
    <select id="getNotice" resultType="com.himedia.groupware.dto.NoticeDto">
        select * from notice where title like concat('%', #{key}, '%') or content like concat('%', #{key}, '%') order by nseq desc limit #{paging.displayRow} offset #{paging.startNum};
    </select>

    <select id="selectNoticeInfo" resultType="com.himedia.groupware.dto.UserDto">
        select * from user where id=#{id}
    </select>

    <insert id="insertNotice" parameterType="com.himedia.groupware.dto.NoticeDto">
        insert into notice(title, userid, content, id, image, savefilename) values (#{title}, #{userid}, #{content}, #{id}, #{image}, #{savefilename} )
    </insert>
    <update id="updateNotice" parameterType="com.himedia.groupware.dto.NoticeDto">
        update notice set title=#{title}, userid=#{userid}, content=#{content}, id=#{id}, image=#{image}, savefilename=#{savefilename} where nseq=#{nseq}
    </update>
    <delete id="deleteNotice" parameterType="com.himedia.groupware.dto.NoticeDto">
        delete from notice where nseq=#{nseq}
    </delete>

    <select id="selectNoticeDetail" parameterType="com.himedia.groupware.dto.NoticeDto">
        select * from notice where nseq=#{nseq}
    </select>

    <select id="selectPay" parameterType="com.himedia.groupware.dto.PayDto">
        select * from pay where id=#{id}
    </select>
    <select id="findUser" parameterType="com.himedia.groupware.dto.UserDto">
        select * from user where id=#{id}
    </select>
    <insert id="insertPay" parameterType="com.himedia.groupware.dto.PayDto">
        insert into pay(id, title, pay, content) values (#{id}, #{paydto.title}, #{paydto.pay}, #{paydto.content})
    </insert>
    <update id="updatePay" parameterType="com.himedia.groupware.dto.PayDto">
        update pay set title=#{paydto.title}, pay=#{paydto.pay}, content=#{paydto.content} where id=#{id}
    </update>


    <select id="currentNotice" resultType="com.himedia.groupware.dto.NoticeDto">
        SELECT * FROM notice ORDER BY indate DESC LIMIT #{num}
    </select>



    <select id="getProvider" resultType="com.himedia.groupware.dto.AsInfoDto">
        SELECT providerid AS id, providername AS name FROM provider
    </select>
    <select id="getPart" resultType="com.himedia.groupware.dto.AsInfoDto">
        SELECT partid AS id, partname AS name FROM part
    </select>
    <select id="getState" resultType="com.himedia.groupware.dto.AsInfoDto">
        SELECT stateid AS id, statename AS name FROM state
    </select>

    <!--프로바이더-->
    <insert id="insertProvider" parameterType="com.himedia.groupware.dto.InfoDto">
        INSERT IGNORE INTO provider(providerid, providername) VALUES (#{id}, #{name})
    </insert>
    <update id="replaceProvider" parameterType="com.himedia.groupware.dto.InfoDto">
        update provider set providername=#{name} where providerid=#{id}
    </update>
    <update id="userReplaceProvider" parameterType="com.himedia.groupware.dto.UserDto">
        update user set provider=0  where provider=#{deleteid}
    </update>
    <delete id="deleteProvider" parameterType="com.himedia.groupware.dto.InfoDto">
        delete from provider where providerid=#{deleteid}
    </delete>

    <!--파트-->
    <insert id="insertPart" parameterType="com.himedia.groupware.dto.InfoDto">
        INSERT IGNORE INTO part(partid, partname) VALUES (#{id}, #{name})
    </insert>
    <update id="replacePart" parameterType="com.himedia.groupware.dto.InfoDto">
        update part set partname=#{name} where partid=#{id}
    </update>
    <update id="userReplacePart" parameterType="com.himedia.groupware.dto.UserDto">
        update user set part=0  where part=#{deleteid}
    </update>
    <delete id="deletePart" parameterType="com.himedia.groupware.dto.InfoDto">
        delete from part where partid=#{deleteid}
    </delete>

    <!--스테이트-->
    <insert id="insertState" parameterType="com.himedia.groupware.dto.InfoDto">
        INSERT IGNORE INTO state(stateid, statename) VALUES (#{id}, #{name})
    </insert>
    <update id="replaceState" parameterType="com.himedia.groupware.dto.InfoDto">
        update state set statename=#{name} where stateid=#{id}
    </update>
    <update id="userReplaceState" parameterType="com.himedia.groupware.dto.UserDto">
        update user set state=0  where state=#{deleteid}
    </update>
    <delete id="deleteState" parameterType="com.himedia.groupware.dto.InfoDto">
        delete from state where stateid=#{deleteid}
    </delete>

    <select id="getNoticeOne" resultType="com.himedia.groupware.dto.NoticeDto">
        select * from notice where nseq=#{nseq}
    </select>

    <update id="update" parameterType="com.himedia.groupware.dto.NoticeDto">
        update notice set title=#{title}, content=#{content}, image=#{image}, savefilename=#{savefilename} where nseq=#{nseq}
    </update>

</mapper>