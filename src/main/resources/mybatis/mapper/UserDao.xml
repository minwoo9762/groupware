<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-/mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himedia.groupware.dao.IUserDao">
    <select id="getUser" resultType="com.himedia.groupware.dto.UserDto">
        select * from user where email=#{email}
    </select>
    <insert id="insertUser" parameterType="com.himedia.groupware.dto.UserDto">
        insert into user(email, pwd, name, phone, part, zipNum, address1, address2, address3, profileimg)
        values(#{email}, #{pwd}, #{name}, #{phone}, #{part}, #{zipNum},
               #{address1}, #{address2}, #{address3}, #{profileimg})
    </insert>
    <update id="updatePwd">
        update user set pwd=#{pwd} where email=#{email}
    </update>
    <update id="updateUser" parameterType="com.himedia.groupware.dto.UserDto">
        update user set name=#{name}, phone=#{phone}, zipNum=#{zipNum},
                        address1=#{address1}, address2=#{address2}, address3=#{address3},
                        profileimg=#{profileimg}
        where email=#{email}
    </update>
    <select id="getAllUser" resultType="com.himedia.groupware.dto.UserDto">
        select * from user order by name;
    </select>
    <select id="isValidName" resultType="boolean">
        select exists(
            select 1 from user where name=#{name}
        )
    </select>
    <select id="getUseridByName">
        select id from user where name=#{name}
    </select>
    <select id="getAllCountForAddress" resultType="_int">
        select count(*) from user where name like concat('%',#{key},'%')
    </select>

    <select id="selectAddress" parameterType="com.himedia.groupware.dto.Paging" resultType="com.himedia.groupware.dto.UserDto">
        select * from user where name like concat('%',#{key},'%') order by name desc limit #{paging.displayRow} offset #{paging.startNum}
    </select>

    <insert id="insert" parameterType="com.himedia.groupware.dto.UserDto">
        insert into user(email, pwd, name, phone, part, address1, address2, provider)
        values(#{email}, #{pwd}, #{name}, #{phone}, #{part}, #{address1}, #{address2}, #{provider})
    </insert>

    <delete id="delete">
        delete from user where id=#{id}
    </delete>

    <select id="getAddress" resultType="com.himedia.groupware.dto.UserDto">
        select * from user where id=#{id}
    </select>

    <update id="updateAddress" parameterType="com.himedia.groupware.dto.UserDto">
        update user set email=#{email}, pwd=#{pwd}, name=#{name}, phone=#{phone}, part=#{part},
                        address1=#{address1}, address2=#{address2}, provider=#{provider}
    </update>
</mapper>