<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.himedia.groupware.dao.AttendanceIDao">
    <select id="selectAttendance" parameterType="com.himedia.groupware.dto.AttendanceDto">
        SELECT * FROM attendance where userid=#{email} ORDER BY aseq DESC LIMIT 1
    </select>
    <select id="selectState" parameterType="com.himedia.groupware.dto.AttendanceDto">
        SELECT state  FROM attendance where userid=#{email} ORDER BY aseq DESC LIMIT 1
    </select>

    <insert id="insertAttendance" parameterType="com.himedia.groupware.dto.AttendanceDto">
        insert into attendance (userid, indate, outdate, state)
        values (#{email}, #{atddto.indate}, #{atddto.outdate}, #{atddto.state});
    </insert>

    <update id="updateAttendance" parameterType="com.himedia.groupware.dto.AttendanceDto">
        UPDATE attendance SET outdate = #{atddto.outdate}, state = #{atddto.state} WHERE userid = #{email} AND outdate IS NULL;
    </update>

    <insert id="insertAtdVct">
        insert into attendance (userid, indate, outdate, state)  values (#{email}, #{startOnly}, #{startOnly}, #{i});
    </insert>

</mapper>