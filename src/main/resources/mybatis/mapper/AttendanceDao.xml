<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.himedia.groupware.dao.AttendanceIDao">
    <select id="selectAttendance" parameterType="com.himedia.groupware.dto.UserDto">
        select * from attendance where userid=#{email};
    </select>

    <insert id="insertAttendance" parameterType="com.himedia.groupware.dto.UserDto">
        insert into attendance (userid, indate, outdate, state)
        values (#{email}, #{atddao.indate}, #{atddao.outdate}, #{atddao.state});
    </insert>

    <update id="updateAttendance" parameterType="com.himedia.groupware.dto.UserDto">
        UPDATE attendance SET outdate = #{atddao.outdate}, state = 2 WHERE userid = #{email} AND outdate IS NULL;
    </update>

</mapper>