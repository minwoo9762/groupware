<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himedia.groupware.dao.IReplyDao">
    <select id="getReplyCount" resultType="_int">
        select count(*) from reply where boardid=#{id}
    </select>

    <insert id="insertBoard" parameterType="com.himedia.groupware.dto.ReplyDto">
        insert into reply(boardid, userid, reply, name) values (#{boardid}, #{userid}, #{reply}, #{name})
    </insert>

    <insert id="insertApp" parameterType="com.himedia.groupware.dto.ReplyDto">
        insert into reply(appid, userid, reply, provider) values (#{appid}, #{userid}, #{reply}, #{provider})
    </insert>

    <select id="selectBoardReply" resultType="com.himedia.groupware.dto.ReplyDto">
        select * from reply where boardid=#{id} order by id desc
    </select>

    <select id="selectAppReply" resultType="com.himedia.groupware.dto.ReplyDto">
        select * from reply where appid=#{id} order by id desc
    </select>

    <delete id="deleteBoard">
        delete from reply where id=#{id}
    </delete>

    <delete id="deleteApp">
        delete from reply where id=#{id}
    </delete>

</mapper>