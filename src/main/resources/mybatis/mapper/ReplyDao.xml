<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himedia.groupware.dao.IReplyDao">
    <select id="getReplyCount" resultType="_int">
        select count(*) from reply where boardid=#{id}
    </select>

    <insert id="insert" parameterType="com.himedia.groupware.dto.ReplyDto">
        insert into reply(boardid, userid, reply, name) values (#{boardid}, #{userid}, #{reply}, #{name})
    </insert>

    <select id="selectReply" resultType="com.himedia.groupware.dto.ReplyDto">
        select * from reply where boardid=#{id} order by id desc
    </select>

    <delete id="delete">
        delete from reply where id=#{id}
    </delete>
</mapper>